# ============================================================================
# ASPMS Backend Environment Variables - RENDER.COM DEPLOYMENT
# ============================================================================

# Node Environment (set to "production" on Render)
NODE_ENV=production

# Server Port (Render automatically sets this, but default is 5000)
PORT=5000

# Session Secret (Render will auto-generate this if using generateValue: true)
# Or manually set a strong random string
SESSION_SECRET=your-super-secret-session-key-change-this

# ============================================================================
# FIREBASE CONFIGURATION (REQUIRED)
# ============================================================================

# Firebase Service Account JSON (entire JSON as a single-line string)
# Get this from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# IMPORTANT: Minify the JSON to a single line before pasting
# Example: {"type":"service_account","project_id":"aspms-pro",...}
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"aspms-pro","private_key_id":"xxx","private_key":"-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n","client_email":"xxx@aspms-pro.iam.gserviceaccount.com","client_id":"xxx","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"xxx"}

# ============================================================================
# CORS CONFIGURATION (REQUIRED)
# ============================================================================

# Allowed Origins (comma-separated list of URLs that can access your API)
# Include both your Firebase Hosting URL and Render backend URL for testing
ALLOWED_ORIGINS=https://aspms-pro.web.app,https://aspms-pro.firebaseapp.com,https://aspms-backend.onrender.com

# ============================================================================
# OPTIONAL CLIENT-SIDE FIREBASE CONFIGURATION
# ============================================================================
# These are used by the frontend (client-side)
# You'll set these as VITE_* variables in your frontend build

# VITE_FIREBASE_API_KEY=your-firebase-api-key
# VITE_FIREBASE_AUTH_DOMAIN=aspms-pro.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=aspms-pro
# VITE_FIREBASE_STORAGE_BUCKET=aspms-pro.appspot.com
# VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
# VITE_FIREBASE_APP_ID=your-app-id
# VITE_FIREBASE_MEASUREMENT_ID=your-measurement-id

# ============================================================================
# BACKEND API URL (for frontend to connect to)
# ============================================================================
# Set this in your frontend .env or vite config
# VITE_API_URL=https://aspms-backend.onrender.com
